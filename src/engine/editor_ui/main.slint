// Main entry point for the editor UI
// All components and globals are organized in separate files

import { GameDebugUI } from "components/windows/game-debug-ui.slint";

// Export the main component for use in Rust
export { GameDebugUI }
